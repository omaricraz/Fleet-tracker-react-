/** Poll interval for `GET /inventory/alerts` while the app shell is mounted. */
export const INVENTORY_ALERTS_POLL_INTERVAL_MS = 5 * 60 * 1000

/** Window label for the bell dropdown (“last N hours”). */
export const INVENTORY_ALERTS_BELL_RECENT_HOURS = 24

/** Browser path for `InventoryAlertsPage` (see `routes/index.tsx`). */
export const INVENTORY_ALERTS_PAGE_PATH = '/alerts'
